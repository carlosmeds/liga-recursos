<div id="top-container" class="container-fluid">
<h1 id="main-title" class="text-center">Perder tempo significa perder vidas</h1>
    <p id="main-subtitle" class="text-center">Conectamos hospitais e fornecedores de maneira mais rápida</p>
  <form id="search-form" class="form-inline" action="/" method="GET">
    <div class="form-group col-md-10">
      <input type="text" class="form-control" id="insumo" name="insumo" placeholder="A Liga sabe quem tem o que você precisa. É só digitar...">
    </div>
    <div class="col-md-2">
      <button type="submit" class="btn btn-primary">Procurar</button>
    </div>
  </form>
</div>
<main>
  <div id="insumos-container" class="container">
    <div class="row">
      <div class="col-md-12">
        {{#if search}}
          <h2 id="insumo-list-title">Buscando ofertas relacionadas com: {{search}}</h2>
        {{else}}
          <h2 id="insumo-list-title">Últimas ofertas</h2>
        {{/if}}
        <ul id="insumo-list" class="list-group">
          {{#each insumos}}
            <li class="list-group-item {{#if dataValues.doacao }}new-insumo{{/if}}">
              <p><span class="forne">{{dataValues.fornecedor}}</span> em <span class="forne">{{dataValues.estado}}</span></p>
              <h2>{{dataValues.title}}</h2>
              <p>Valor por unidade: {{dataValues.valor_un}}</p>
              <span class="new-insumo-label">Doação</span>
              <a class="btn btn-primary" href="/insumos/view/{{dataValues.id}}">Saiba mais</a>
            </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</main>